# 支付宝小程序项目约束要点

## 项目结构

- 推荐在 `src/platform/alipay` 进行能力差异封装。
- 通用页面/组件不依赖 `my` API；仅 adapter 层依赖平台对象。

## 路由和入口

- 按 `app.js`/`app.json`（或等效配置）统一声明页面、tab、主题和权限。
- 需要外部能力时在入口统一声明，避免运行时动态补充导致签名不一致风险。

## 数据与配置

- `request`、`storage`、`navigateTo` 等平台能力通过统一 adapter 转换。
- 域名与接口地址由运行时配置注入，不硬编码环境。
