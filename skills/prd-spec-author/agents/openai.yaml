interface:
  display_name: "PRD Spec Author"
  short_description: "从结构化需求生成并修订 PiFlow 共享 PRD 总源文档，带覆盖与质量复审"
  default_prompt: "Use $prd-spec-author to update output-stages/prd/prd-spec.md from canonical req or inputs/req.yaml, preserve valid content, correct requirement conflicts, and finish with source coverage plus shared-spec quality review loops."

policy:
  allow_implicit_invocation: true
